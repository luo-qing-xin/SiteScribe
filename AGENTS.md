# 工地小秘工程约定

- 保留 Evidence First 原则：不得改写用户原始现场描述，不得生成用户未输入的施工事实。
- 所有时间由后端以 UTC 保存，前端仅在展示时转换到本地时区。
- 业务 API 必须同时校验登录状态和项目成员身份；不能只依赖前端校验。
- 照片只能保存到 `backend/data/uploads/`，数据库只存相对路径和元数据。
- 数据库结构变更必须提供 Alembic 迁移。
- 前端保持移动端优先，并重点检查 375px 宽度。
- 提交前运行 `make check`；核心交互改动还需运行 `make test-e2e`。

